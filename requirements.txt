# =============================================================================
#  Nairobi Urban Flood Digital Twin — Pinned Dependencies
#  Target runtime: Python 3.10+
#  Version policy: compatible-release (~=) or minimum-floor (>=) per
#                  TECH_STACK.md § Version Pinning Policy
#  Install: pip install -r requirements.txt
# =============================================================================

# ---- PyTorch CPU-only index (avoids downloading ~2 GB CUDA wheels) ----
--extra-index-url https://download.pytorch.org/whl/cpu

# ---- Geospatial Libraries ----
earthengine-api>=1.4.0
geemap>=0.35.0
geopandas>=1.0.0
rasterio>=1.4.0
shapely>=2.0.0
pyproj>=3.7.0
fiona>=1.10.0

# ---- Deep Learning (CPU-only for local dev — MEMORY_CONSTRAINTS.md) ----
torch>=2.6.0
torchvision>=0.21.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.5.0

# ---- Dashboard & Visualisation ----
dash>=2.18.0
dash-bootstrap-components>=1.6.0
plotly>=5.24.0
pydeck>=0.9.0

# ---- Utilities & Infrastructure ----
python-dotenv>=1.0.0
psutil>=6.0.0
loguru>=0.7.0
pyyaml>=6.0.0
requests>=2.32.0
tqdm>=4.67.0
