# =============================================================================
#  Runtime dependencies for the dashboard container only.
#
#  requirements.txt covers the whole project, including the offline data
#  pipeline (Earth Engine, geemap, geopandas), which the dashboard never imports.
#  This list is what `import src.dashboard.app` actually loads, pinned to the
#  versions the dashboard was tested with, so the image is small and
#  reproducible.
# =============================================================================

# PyTorch CPU wheels. Pinning the +cpu local version forces this index; the
# default PyPI Linux wheel bundles ~2 GB of CUDA libraries the container cannot use.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu

dash==4.1.0
dash-bootstrap-components==2.0.4
plotly==6.7.0
pydeck==0.9.2

numpy==2.4.6
scipy==1.17.1
shapely==2.1.2
rasterio==1.5.0

loguru==0.7.3

# Production WSGI server (the Flask development server is not for deployment).
gunicorn==23.0.0
